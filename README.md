# Idempotent-Calculator
Program to test two different algorithms for computing an arbitrary iteration of an idempotent function with finite domain. Both algorithms perform a precomputation before queries are made. The first algorithm queries in less time, but the second uses less space.
